{{- if .Values.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "auth-service.fullName" $ }}-svc
spec:
  type: {{ .type }}
  selector:
    {{- include "auth-service.selectorLabels" $ | nindent 4 }}
  ports:
    - port: {{ .port }}
      targetPort: {{ $.Values.container.port }}
{{- end }}
