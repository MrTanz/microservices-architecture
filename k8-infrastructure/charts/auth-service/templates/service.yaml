{{- range $idx, $svc := (default list .Values.services) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "auth-service.fullName" $ }}-svc-{{ $idx }}
spec:
  type: {{ .type }}
  selector:
    {{- include "auth-service.selectorLabels" $ | nindent 4 }}
  ports:
    - port: {{ .port }}
      targetPort: {{ $.Values.container.port }}
{{- end }}
